# Development Notes

## Formatting

This repository uses Black for Python formatting, configured in `pyproject.toml`.

The configured line length is 120 characters to match the repository conventions.

Run formatting from the repository root:

```bash
black .
```
